// import { select } from '@clack/prompts';

// export async function promptSongSelection(hits) {
//   const options = hits.map((hit, index) => ({
//     value: hit.result,
//     label: `${index + 1}. ${hit.result.full_title}`
//   }));

//   return await select({
//     message: '🎵 Choose a song:',
//     options
//   });
// }
// not functional